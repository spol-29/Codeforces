#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin >> s;
    int n;
    vector<string> v;
    cin >> n;
    while(n--){
        string t;
        cin >> t;
        v.push_back(t);
    }
}